.TH _PRINTF 3 "January 2025" "Holberton School" "User Manual"

.SH NAME
_printf \- custom implementation of the printf function

.SH SYNOPSIS
.B #include "main.h"
.br
.B int _printf(const char *format, ...);

.SH DESCRIPTION
The
.B _printf()
function produces output according to a format string.
This function writes its output to the standard output stream (stdout).

The format string is composed of zero or more characters, and zero or
more conversion specifiers beginning with the '%' character.

This custom implementation of printf handles the following
conversion specifiers:

.TP
.B %c
Prints a single character.

.TP
.B %s
Prints a string of characters. If the string argument is NULL,
"(null)" is printed.

.TP
.B %%
Prints a literal percent sign.

.TP
.B %d, %i
Prints a signed decimal integer.

.PP
This implementation does not handle:
.IP \[bu] 2
Flags
.IP \[bu] 2
Field width
.IP \[bu] 2
Precision
.IP \[bu] 2
Length modifiers
.IP \[bu] 2
Buffer handling identical to the standard printf

.SH RETURN VALUE
The
.B _printf()
function returns the number of characters printed, not including
the terminating null byte.

If the format string is NULL, the function returns -1.

.SH EXAMPLES
.nf
_printf("Hello %s\n", "World");
     Output: Hello World

_printf("Number: %d\n", 123);
     Output: Number: 123

_printf("Percent: %%\n");
     Output: Percent: %
.fi

.SH SEE ALSO
printf(3)

.SH AUTHORS
Kris, Jul, and Koralys — Holberton School
